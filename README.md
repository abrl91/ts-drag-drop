# ts-drag-drop

### Requirements:

TypeScript installed globally
```
npm install -g typescript
```

### Run On Dev

In the terminal run:

```
tsc -w
```

Open another terminal tab and run:
```
npm run start
```
